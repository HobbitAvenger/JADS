[gd_scene load_steps=4 format=2]

[ext_resource path="res://Player/Player.tscn" type="PackedScene" id=1]
[ext_resource path="res://Scenes/House1.tscn" type="PackedScene" id=3]

[sub_resource type="CSharpScript" id=1]
script/source = "//I put this here to test what was breaking the script loader. I can't figure out how to remove it. Sorry

using Godot;
using System;

public class House1 : TileMap
{
	// Declare member variables here. Examples:
	// private int a = 2;
	// private string b = \"text\";

	// Called when the node enters the scene tree for the first time.
	public override void _Ready()
	{
		GD.Print(\"Loaded tile map script!\");
	}

//  // Called every frame. 'delta' is the elapsed time since the previous frame.
//  public override void _Process(float delta)
//  {
//      
//  }
}
"

[node name="MainMenu" type="Node2D"]

[node name="House1" parent="." instance=ExtResource( 3 )]
script = SubResource( 1 )

[node name="Player" parent="." instance=ExtResource( 1 )]
position = Vector2( 510, 0 )
